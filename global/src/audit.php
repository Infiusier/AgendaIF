<?php
	

	# aplicação de auditoria
	/*
	$db = new DB();
	if( Session::auth() ){
		$user = Session::get();
		$aud['usuario_id'] = $user["id_usuario"];
		$aud['auditoria_servidor'] = json_encode($_SERVER);
		$aud['auditoria_requisicao'] = json_encode($_REQUEST);
		$db->insert("vth_auditorias",$aud,null);
	}
	*/
?>